class player1:

    def __init__(self, x_pos, y_pos):
        self.x = x_pos
        self.y = y_pos

class player2:
    def __init__(self, x_pos, y_pos):
        self.x = x_pos
        self.y = y_pos

class ball:
    def __init__(self, x_pos, y_pos, speed):
        self.x = x_pos
        self.y = y_pos
        self.kiirus = speed
